<%= link_to company_project_path(project), data: {toggle: "modal", target: "#project-modal"}, class: 'card-toggler' do %>
  <div class="card-company-project">

    <div class="company-project-status">
      <div class="backg-status" style="background-color: <%= project.status[:color] %>">
        <div class="cp-status"><%= project.status[:label] %></div>
      </div>
    </div>

    <div class="company-project-infos">
      <div class="cp-title"><strong>Projet : </strong><%= project.request_title %></div>

      <div class="cp-btn-date">
        <div class="cp-date"><strong>Envoyé le </strong><%= project.created_at.strftime("%d/%m/%Y") %></div>
      </div>

    </div>
  </div>
<% end %>

